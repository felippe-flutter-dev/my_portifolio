/// Project layout policy, in logical pixels of the available constraints.
/// Keep the established portfolio composition at 900; this is not an OS test.
abstract final class AppBreakpoints {
  static const double expanded = 900;
}
